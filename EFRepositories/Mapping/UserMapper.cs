using AutoMapper;
using EntityFrameworkContext.Entities;
using Infrastructure.Entities;

namespace EFRepositories.Mapping
{
    internal static class UserMapper
    {
        //static UserMapper()
        //{
        //    Mapper.Initialize(cfg => cfg.CreateMap<IUser, User>());
        //    Mapper.Initialize(cfg => cfg.CreateMap<User, IUser>());
        //}

        //public static User Map(IUser user)
        //{
        //    return Mapper.Map<IUser, User>(user);
        //}

        //public static IUser Map(User dalUser)
        //{
        //    return Mapper.Map<User, IUser>(dalUser);
        //}
    }
}
